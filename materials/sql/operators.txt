select - ВЫБЕРИ
* - все колонки
from - из
employees - таблицы с именем employees
DISTINCT - используется для удаления дубликатов из результирующего набора данных
WHERE - оператор указывающий, что оператор языка управления данными должен действовать только на записи,
        удовлетворяющие определенным критериям. {WHERE city = 'London';}


select * from employees limit 10    вернуть из таблицы employees первые 10 записей